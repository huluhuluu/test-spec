# 记录

## 目标

对以下 EAGLE-3 draft model 做统一抽样评测，每个数据集抽取 80 条：

- `AngelSlim/Qwen3-1.7B_eagle3`
- `AngelSlim/Qwen3-4B_eagle3`
- `taobao-mnn/Qwen3-4B-Instruct-2507-Eagle3`
- `Zjcxy-SmartAI/Eagle3-Qwen3-4B-Instruct-2507-zh`
- `AngelSlim/Hunyuan-1.8B-Instruct_eagle3`
- `AngelSlim/Hunyuan-4B-Instruct_eagle3`

数据集：

- `gsm8k`
- `math500`
- `mtbench`
- `humaneval`
- `ceval`
- `cmmlu`

## 当前实现

主脚本是 [run_eval.py](/workspace/code/test-eagle3/run_eval.py:1)。

它包含 4 类能力：

1. 数据准备
2. 模型下载
3. 4 卡并行调度
4. 推理结果、投机解码 trace、数据集汇总输出

数据抽样现在默认是“只做一次”：

- 如果 `artifacts/samples/<dataset>.jsonl` 已存在，则直接复用
- 不会在后续 `run` 或 `tmux` 重启时重新拉取/抽样
- 只有显式加 `--force-resample` 才会重新抽样

## 固定投机解码配置

按你的最新要求，脚本固定使用：

- `speculative_num_steps = 7`
- `speculative_eagle_topk = 10`
- `speculative_num_draft_tokens = 32`

另外为了避免 `topk > 1` 时不同 attention backend 的不稳定组合，当前 engine 配置里把 `page_size` 固定成了 `1`。这偏向正确性和兼容性，不是绝对最快配置。

另外这次评测把运行时 `context_length` 固定为 `1024`，并设置：

- `SGLANG_ALLOW_OVERWRITE_LONGER_CONTEXT_LEN=1`

原因是部分 `Qwen3/Hunyuan + EAGLE3 draft` 组合的配置里，base model 声明的最大上下文远大于 draft worker 推导值；如果不覆盖，SGLang 会在初始化阶段直接拒绝启动。对这次 benchmark 抽样测试，这里改成更保守的主流长度配置：

- 默认 `max_new_tokens = 512`
- `mtbench` / `humaneval` 使用 `max_new_tokens = 1024`
- engine `context_length = 1024`

## 下载方式

模型下载目录固定为：

- `/data/HUGGINGFACE`

Hugging Face 下载固定使用：

- `/data/HUGGINGFACE/hfd.sh`
- `HF_ENDPOINT=https://hf-mirror.com`

不使用代理下载模型。

CMMLU 由于当前 `datasets` 版本不再支持它的 dataset script，改为从官方 GitHub 仓库拉 CSV。这个下载脚本会只在 clone 期间设置代理，结束后 `unset`：

- [scripts/download_cmmlu.sh](/workspace/code/test-eagle3/scripts/download_cmmlu.sh:1)

## 运行方法

默认使用 `spec` 环境里的 Python：

```bash
cd /workspace/code/test-eagle3
/root/miniconda3/envs/spec/bin/python run_eval.py download-models
/root/miniconda3/envs/spec/bin/python run_eval.py download-cmmlu
/root/miniconda3/envs/spec/bin/python run_eval.py prepare-data --sample-size 80
/root/miniconda3/envs/spec/bin/python run_eval.py run --sample-size 80 --gpus 0 1 2 3
```

如果需要重新抽样：

```bash
/root/miniconda3/envs/spec/bin/python run_eval.py prepare-data --sample-size 80 --force-resample
```

如果只想做数据集抽样和链路自检：

```bash
/root/miniconda3/envs/spec/bin/python run_eval.py smoke --sample-size 3
```

## 输出目录

所有输出都在 `artifacts/` 下：

- `artifacts/samples/*.jsonl`
  - 每个 benchmark 的 80 条抽样样本
- `artifacts/logs/<model>/combined_results.jsonl`
  - 该模型跨所有数据集的统一结果日志
- `artifacts/logs/<model>/<dataset>/results.jsonl`
  - 单个数据集逐条结果
- `artifacts/logs/<model>/spec_trace_raw.jsonl`
  - 原始 speculative trace
- `artifacts/logs/<model>/<dataset>/spec_trace.jsonl`
  - 只保留该数据集 request 的 speculative trace
- `artifacts/logs/<model>/<dataset>/summary.json`
  - 单数据集汇总
- `artifacts/reports/run_summary.json`
  - 所有模型汇总
- `artifacts/reports/run_summary.md`
  - Markdown 汇总

## 记录的日志内容

每条推理结果日志会记录：

- 原始输出文本
- 数据集名、样本 ID、模型名
- `spec_accept_length`
- `spec_accept_rate`
- `spec_verify_ct`
- 逐数据集打分结果

每条 speculative trace 会记录：

- `rid`
- `accept_len`
- `accepted_token_ids`
- `accepted_tokens`
- `rejected_candidate_token_ids`
- `rejected_candidate_tokens`
- `draft_chunk_token_ids`
- `draft_chunk_tokens`

这里的 `rejected_candidate_*` 指该次 draft chunk 中没有进入连续接受前缀的候选 token。它是最接近“被拒绝 token”的可追踪日志形式。

## 已知限制

1. `mtbench`

当前默认是 generation-only 模式，保存两轮回答和 trace，但**没有接入 judge model**，所以不会给出标准 MT-Bench 分数。要拿标准分，需要再接一套评审模型或 FastChat judge 流程。

2. `humaneval`

当前是 `pass@1` 风格的单样本执行评测，适合这次 80 条抽样测试。不是多次采样下的正式 `pass@k`。

3. `ceval/cmmlu`

当前实现是零样本选择题提示，而不是 few-shot 官方协议。这样实现更直接，也更适合先比较 speculative decoding 行为和基础正确率。

## 本轮已验证

这次我实际验证过：

- `spec` 环境内存在 `sglang 0.5.6 / transformers 4.57.1 / torch 2.9.1`
- `gsm8k / math500 / mtbench / humaneval / ceval` 可以直接用 `datasets` 拉取
- `cmmlu` 需要走 GitHub CSV fallback
- CMMLU 官方仓库结构和 CSV 字段已确认可读

还没有实际跑完整模型评测，因为本轮主要完成的是代码、下载/调度链路和日志结构。

## 官方后端策略（2026-04-30 更新）

- 不再沿用“所有模型统一走一个 SGLang 推理栈”的方案。
- `AngelSlim/Qwen3-1.7B_eagle3`
- `AngelSlim/Qwen3-4B_eagle3`
- `AngelSlim/Hunyuan-1.8B-Instruct_eagle3`
- `AngelSlim/Hunyuan-4B-Instruct_eagle3`
  以上 4 个模型改为按 AngelSlim 官方文档走 `vLLM + tools/vllm_spec_benchmark.py` 路线。
- `Zjcxy-SmartAI/Eagle3-Qwen3-4B-Instruct-2507-zh`
  按模型卡给出的 `SGLang` 方式启动与测试。
- `taobao-mnn/Qwen3-4B-Instruct-2507-Eagle3`
  按 `SpecForge / SGLang-ready` 官方路径处理，不再混进 AngelSlim/vLLM 或旧统一引擎。

## 官方脚本与采集需求的差距

- AngelSlim 官方 `vLLM` benchmark 原生提供：
  - 吞吐
  - 平均接受长度
  - 各 speculative 位置接受率
- SpecForge 官方 `SGLang` benchmark 原生提供：
  - `gsm8k / math500 / mtbench / humaneval / ceval`
  - 接受长度与吞吐
- 但两条官方路径都不原生提供：
  - 接受长度分布计数
  - 连续 accepted token 序列
  - rejected token 候选日志
  - `cmmlu`
- 后续实现将保持“官方后端 + 最小侵入日志补丁”，而不是回到自定义统一推理方案。

## 依赖环境策略

- `spec` 环境继续保留给 `SGLang`，当前版本是 `torch 2.9.1+cu128 / sglang 0.5.6`
- 直接在 `spec` 环境安装新版 `vLLM` 会尝试切到 `torch 2.11 / CUDA 13`，存在污染 `SGLang` 运行栈的风险
- 因此 `AngelSlim/vLLM` 官方路径需要独立环境，不与 `SGLang` 共用同一套依赖
- 独立环境准备脚本： [scripts/setup_angelslim_vllm_env.sh](/workspace/code/test-eagle3/scripts/setup_angelslim_vllm_env.sh:1)
- `tmux` 启动脚本已更新，会在正式评测前先准备该环境： [scripts/start_eval_tmux.sh](/workspace/code/test-eagle3/scripts/start_eval_tmux.sh:1)

## 当前已验证的新链路

- 主调度器已改为“每个模型一个独立子进程”，按模型后端选择不同 Python 环境
- `run-model` 子命令已加入 [run_eval.py](/workspace/code/test-eagle3/run_eval.py:1)
- `Zjcxy-SmartAI/Eagle3-Qwen3-4B-Instruct-2507-zh` 已按 `SGLang` 官方路线启动真跑验证
- 已确认：
  - GPU0 显存被实际占用
  - 结果文件已开始写入 `artifacts/logs/zjcxy_qwen3_4b_eagle3_zh/gsm8k/results.jsonl`
  - 单条结果已包含 `score / spec_accept_length / spec_accept_rate / spec_verify_ct`
